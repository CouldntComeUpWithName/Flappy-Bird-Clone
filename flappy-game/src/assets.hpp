#pragma once

namespace flappy
{
  enum class texture_type
  {
    BIRD_SHEET,
    VANILLA_BACKGROUND,
    VANILLA_PIPE,
  };

  enum class sound_type
  {
    FLAP,
    FALL,
    BUMP,
    GAME_OVER,
    CLICK,
  };
}