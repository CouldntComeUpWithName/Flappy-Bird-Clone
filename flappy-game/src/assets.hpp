#pragma once

namespace flappy
{
  enum class texture
  {
    BIRD_SHEET,
    VANILLA_BACKGROUND,
    VANILLA_PIPE,
  };

  enum class sound
  {
    FLAP,
    FALL,
    BUMP,
    GAME_OVER,
    CLICK,
  };
}