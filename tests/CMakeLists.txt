if(BUILD_TESTING)
  if(MSVC)
    get_scoped_target_list(tests)

    foreach(target IN LISTS tests)
      if(TARGET ${target})
        set_target_properties(${target} PROPERTIES FOLDER "Tests")
      endif()
    endforeach()
  endif()
endif()
